# Game Daily — MVP 统一实施清单 v0.3

状态：本机初版已实现，实际早报生成已跑通；真实语音与公网部署待验收。详见 IMPLEMENTATION-STATUS.md。
日期：2026-09-10

## 1. 依据与范围

依据 Game_Industry_Radio_Codex_Specification_Pack_v0.2.docx（交互与工程）、Game_Daily_Editorial_Direction_Content_Selection_Rules.docx（编辑规则），以及用户在对话中确认的自动生成需求与补充约定。

用户确认的自动生成需求覆盖原 v0.2 将新闻采集放在第二阶段的限制。文档中的启动指令本身不构成执行授权。

产品面向未来独立游戏开发者与游戏设计师。打开网页自动加载当天早报；当天没有早报时自动检索、生成并保存。用户不需要操作 JSON。点击开始后支持播报、打断、上下文问答、等待、明确继续后恢复。

## 2. 架构与六个模块

采用一个 Next.js / React / TypeScript 项目，前后端一起维护。模块是代码职责边界，无需分别部署。

1. 新闻检索：按编辑方向检索，输出包含来源、发布时间和可核对材料的候选内容。
2. 早报编辑：去重、核对、评分、选择、分析，输出通过校验的 MorningBrief。
3. 当天早报服务：负责日期、读取、生成状态、并发去重、持久保存、失败重试。
4. 播报控制器：负责章节与段落进度、暂停、打断、恢复、完成；独立于模型和界面。
5. 语音连接：负责 Realtime WebRTC、声音、文字事件和连接错误；长期 API 密钥只在服务端。
6. 对话界面：Today 导航、对话流、新闻/设计卡片、语音状态、统一输入栏。

依赖方向：检索 → 编辑 → 早报服务与存储 → 界面与播报控制器 ↔ 语音连接。
编辑提示词和主播提示词分开，模型、声音与支持的配置集中管理。

## 3. 内容生成流程

检索 → 候选池 → 去重与核对 → 评分 → 选择 → 分析 → JSON 校验 → 保存。
不得直接把首批搜索结果当作最终早报。

四个方向：
- 独立开发工具与工作流，最高优先级。
- 大厂与平台方向。
- 游戏创作者与设计思考。
- 独立开发案例与复盘。

评分依据：用户相关性、实际可迁移性优先；兼顾行业意义、设计学习价值、新颖性。来源质量是入选门槛。具体数值权重是实现细节，不伪装成客观评分。

每天目标 5–7 条，质量优先，允许不足；默认平衡为工具 1–2、行业 1–2、设计 1–2、独立案例 0–1，不硬凑配额。
过滤无意义营销、重复报道、无实际用途的工具广告和缺少游戏开发关联的泛 AI 消息。

优先官方公告、开发者博客、直接访谈、演讲、复盘与可靠专业报道。重大策略结论交叉核对。保留真实 URL；来源正文不支持的结论不得写成事实。格式校验不等于事实核验。

## 4. 已确认的编辑补充

- 行业新闻优先最近 24–48 小时；工具、访谈、复盘可放宽，保留真实日期。
- 跨日保存选题记录，重复事件只有实质更新才重新入选。
- 不足五条时发布短版，说明内容较少；没有合格内容时不伪造新一期。
- 每期结尾包含 Today’s Signal / 今日观察，属于分析且不计入新闻条数。证据不足时不强行建立趋势。
- 目标播报约 8–12 分钟，实际长度需语音测试校准，更多分析留给追问。
- 默认以 Australia/Sydney 时区确定当天；使用时区规则处理夏令时。

## 5. 数据约定

MorningBrief 包含：schemaVersion、id、version、date、timezone、generatedAt、locale、title、items、todaysSignal。
每条包含：稳定 id、分类、标题、事实摘要、重要性、用户相关性、来源、短播报段落。
来源包含：标题、URL、发布者、可取得的发布日期与来源类型；未知日期不得编造。

设计内容增加：设计问题、设计决策、玩家行为、玩家体验、可借鉴结论。
工具内容增加：开发问题、旧流程、新流程、改善点、限制、是否值得尝试。
事实、推测、编辑分析应可区分。每条关键事实应能对应支持它的来源；不能把推测或宣传写成实测结果。

内部候选记录保存去重依据、评分和入选理由，不强制显示在用户界面。
进度绑定 briefId + version，包含当前章节与段落、已完成段落、打断来源和播报/问答模式。

## 6. 自动加载与保存

打开 → 获取当天早报状态 → ready 时显示；missing 时请求创建生成任务；generating 时显示进度并读取状态；failed 时允许重试。

按日期、时区与编辑配置识别同一生成任务，防止刷新、多标签页或重复点击造成并行付费生成。生成工作不能仅依赖页面保持连接；需按部署平台选择可持续运行的任务方式。
仅在生成结果校验成功后发布，失败不能覆盖已有有效早报。过期任务需能恢复或重试。
上线使用持久存储保存早报、生成状态与近期选题记录，不依赖临时文件或进程内存。具体存储与任务机制在部署环境确定后选择。
首次访问触发生成属于 MVP；定时提前生成以后调用相同入口即可。
公开部署前明确个人访问边界，并对付费生成和语音会话设置服务端限流与使用上限，避免公开接口被无限调用。

## 7. 交互与恢复规则

桌面轻量 Today 导航；手机导航默认收起。内容与文字转录放在同一对话流。输入栏同时支持文字与语音。

状态：空闲、连接中、就绪、播报中、被打断、回答中、暂停、错误、结束。
打断立即停止当前播报并冻结进度；回答后等待明确继续，不自动接播。问答不能推进早报进度。
恢复以短段落为单位：可重说未完成的短段落，已完成段落不重复；不承诺逐字无缝恢复。
不能仅凭模型发出完成信号推进进度；需结合实际输出播放状态确认，防止已生成但未听完的内容被跳过。
重复继续不得重复启动输出；过期事件不得修改新一轮会话进度。断线保留本地进度，重连不从第一节重来。
首次麦克风与声音启动由用户操作触发。提供暂停、停止、静音、重试与可访问标签。
问答依据早报及随附材料；超出材料时说明不足，MVP 不默认开启现场外部研究。

## 8. 五阶段、十步骤与验收

阶段一：基础（粗估 0.5–1 天）
[x] 1. 检查项目、确认部署约束、整理规格差异。
[x] 2. 建立数据契约、段落格式与模块接口，校验样例。
验收：样例校验通过，非法数据被拒绝，模块职责清晰。

阶段二：自动早报（粗估 1–2 天，编辑质量验证可能增加工作量）
[x] 3. 实现检索、候选池、核对、评分、选择与分析。
[x] 4. 实现持久保存、自动加载、生成状态与并发去重。
验收：首次访问得到带真实来源的当日早报；再次访问复用；失败可重试；跨日去重生效。

阶段三：界面与进度（粗估 1–1.5 天）
[x] 5. 实现桌面与手机对话界面。
[x] 6. 实现与测试独立播报控制器。
验收：内容和来源可查看；模拟打断、连续继续、暂停及停止时进度正确。

阶段四：实时语音（粗估 1.5–3 天）
[ ] 7. 核实当前官方接口与账户可用模型，安全接入真实语音。
[ ] 8. 完成播报、打断、回答、等待、明确继续的完整链路。
验收：至少三段真实可听播报；章节开始、中途、末尾打断均不跳过未听完内容。

阶段五：验证与交付（粗估 1–2 天）
[ ] 9. 测试失败场景、真实浏览器与手机表现。
[ ] 10. 完成部署配置、使用说明与验收报告。
验收：lint、类型检查和自动化测试通过；实际执行打断→回答→继续；列明浏览器、限制与未验证项。

总量先按约 5–10 个工作日估算，尚未检查项目和部署环境，不构成确定交付期限；编辑质量与真实音频联调需预留余量。

## 9. 必须验证的风险场景

- 新闻来源缺失、发布时间不明、重复报道、事实与来源不符、合格内容不足。
- 跨日、夏令时、多标签页同时访问、任务超时、失败重试、旧早报回退。
- 拒绝麦克风、WebRTC 失败、网络断开、答案播放时暂停、Stop 后 Start。
- 重复继续、连续打断、章节末尾打断、过期完成事件与未播放完的音频。
- 密钥不进入浏览器、日志或提交；开发测试尽量用模拟事件，真实 API 验收单独运行。

## 10. 暂不实施

账号产品、订阅支付、复杂推荐、向量数据库、原生手机应用、MP3 导出、个人历史页面、设置中心、自动保存设计笔记，以及传统播客播放器控件。

后续可扩展：定时生成、RSS 或其他检索来源、早报历史、用户批准的设计笔记。通过现有模块接口接入。
