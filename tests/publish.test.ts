import test from "node:test";import assert from "node:assert/strict";import {citationKey,publishDraft} from "../lib/editorial/publish";import type {BriefItem} from "../lib/brief/schema";
const item:BriefItem={id:"a",category:"industry",headline:"Test",summary:"Test",whyItMatters:"Test",userRelevance:"Test",sources:[{title:"Source",publisher:"Publisher",url:"https://example.com/article",publishedAt:null,kind:"primary"}],paragraphs:["Test"],uncertainty:null,design:null,workflow:null};
test("tracking parameters can normalize without changing the source page",()=>{assert.equal(citationKey("https://example.com/article?utm_source=demo#section"),citationKey(item.sources[0].url));assert.notEqual(citationKey("https://example.com/other"),citationKey(item.sources[0].url));});
test("untraceable story and its synthesis are omitted, verified material remains",()=>{const bad={...item,id:"b",sources:[{...item.sources[0],url:"https://unseen.example/article"}]};const brief=publishDraft({title:"Test",items:[item,bad],todaysSignal:{text:"Pattern connecting both",itemIds:["a","b"]},editorNote:""},new Set([item.sources[0].url]),"2026-09-10","Australia/Sydney");assert.equal(brief.items.length,1);assert.deepEqual(brief.todaysSignal.itemIds,[]);assert.throws(()=>publishDraft({title:"Test",items:[bad],todaysSignal:{text:"None",itemIds:[]},editorNote:""},new Set(),"2026-09-10","Australia/Sydney"));});
