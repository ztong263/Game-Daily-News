import {api} from "../lib/server/openai";import {candidateSchema} from "../lib/editorial/selection";import {contentSchema} from "../lib/brief/schema";import {editorialFormat} from "../lib/editorial/format";
async function main(){for(const [name,schema]of [["candidates",candidateSchema],["brief",contentSchema]] as const){try{await api().responses.create({model:process.env.EDITORIAL_MODEL||"gpt-6-astra",reasoning:{effort:"low"},input:"Return an empty list where possible.",max_output_tokens:32,text:{format:editorialFormat(schema,name)}},{timeout:30000,maxRetries:0});console.log(name+" schema accepted");}catch(error){const e=error as {code?:string;message?:string;status?:number};console.log(JSON.stringify({name,status:e.status,code:e.code,detail:e.code==="invalid_json_schema"?e.message:undefined}));}}}void main();
